[package]
name = "noir_bigcurve"
type = "lib"
authors = [""]
compiler_version = ">=1.0.0"

[dependencies]
bignum = { path = "../bignum" }
poseidon = { tag = "v0.1.1", git = "https://github.com/noir-lang/poseidon" }
