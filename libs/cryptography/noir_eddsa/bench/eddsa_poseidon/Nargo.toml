[package]
name = "noir_eddsa_poseidon"
type = "bin"
authors = [""]
compiler_version = ">=0.36.0"

[dependencies]
eddsa = { path = "../../lib" }
poseidon = { tag = "v0.1.1", git = "https://github.com/noir-lang/poseidon" }
